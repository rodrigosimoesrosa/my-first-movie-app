<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:name=".application.MoviesApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyFirstAppMovie"
        tools:targetApi="31">

        <activity android:name=".oldway.main.OldMainActivity"
            android:exported="true"
            android:theme="@style/Theme.MyFirstAppMovie">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".oldway.genres.OldGenresActivity"
            android:exported="true"
            android:theme="@style/Theme.MyFirstAppMovie">
        </activity>

        <activity
            android:name=".oldway.movies.OldMoviesActivity"
            android:exported="true"
            android:theme="@style/Theme.MyFirstAppMovie" />

        <activity
            android:name=".oldway.movie.OldMovieActivity"
            android:exported="true"
            android:theme="@style/Theme.MyFirstAppMovie" />

        <activity android:name=".oldway.signin.OldSignInActivity"
            android:exported="true"
            android:theme="@style/Theme.MyFirstAppMovie"/>

        <activity android:name=".oldway.terms.OldTermsActivity"
            android:exported="true"
            android:theme="@style/Theme.MyFirstAppMovie"/>

        <activity android:name=".oldway.signup.OldSignUpActivity"
            android:exported="true"
            android:theme="@style/Base.Theme.MyFirstAppMovie"/>

        <activity android:name=".oldway.search.OldSearchActivity"
            android:exported="true"
            android:theme="@style/Base.Theme.MyFirstAppMovie"/>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>